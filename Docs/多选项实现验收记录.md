# 多选项实现验收记录

2026-09-23，本次实现使用 Unity 6000.6.0f1 的独立验证工程完成原生检查：**24 项通过，0 项失败**。正式工程当前打开的 Unity 和原玩家存档未参与写入测试。

## 已验证的功能

- 第一次挥棒之前出现路线选择。原著后续正文保留，移除了两组旧观察循环的六个节点；既有节点文字只有原出手段落因拆分入口而调整。
- 十二条娱乐测试路线加原著、完美测试路线形成十四个选项；增删、草稿、停用和排序正确影响列表。
- 中文按钮清晰，长列表纵向滚动，导航到最后一项会自动滚入视野；重复提交只进入一次，暂停后恢复选择正常，工具栏处于选项面板上方。
- 错误跳转、无出口循环、跨路线私有节点跳转、重复正经路线均被拒绝。
- 原生 JsonUtility 的 schema 3 继承字段、道具、完整非递归检查点能往返；没有检查点的开场档可读；旧 v2 在内存迁移且读取不改写源文件。
- 非法格式、错误版本、无效道具参数和损坏检查点在恢复前被拒绝。
- 实际 VNMain 场景中的娱乐路线可到达带 B03 的共享终点，正常标记本轮结束，同时不增加主线完成、纪念或收藏记录。
- 删除路线后，实际 StoryRunner 恢复选择前的历史、视觉和运行状态，显示恢复提示，并能继续重新选择。
- 原生编辑器成功生成正式目录；发布索引只有 canonical，完美草稿和十二条娱乐草稿没有混入。

全部正式 C# 源文件与通过验证的副本逐文件哈希一致，Git 差异检查通过。另已使用工程实际 Unity 程序集编译 Runtime 和 Editor；独立资源逻辑检查 38 项、存档数据逻辑检查 28 项通过。

## 内容边界

正式可玩路线目前只有原著线。`perfect` 与 `fun_01` 至 `fun_12` 是内容包草稿，等待实际分支剧本和美术。十四选项测试使用隔离的合成数据，未发布为正式剧情。

美术动作已提供变身、闪白、震动、道具显示/清除和等待。正式新分支尚未交付，因此这些分支的完整演出节奏、全部美术效果、目标手机触控和性能仍需在内容接入后验收；本次没有声称完成这些内容验收。

## 重复检查入口

- 正式目录：`BaiguVN → Routes → Rebuild Published Catalog`。
- 无副作用核心检查：`BaiguVN → 验证 → 路线集成检查`。
- 完整逐帧检查使用独立验证副本，调用 `BaiguVN.Editor.RouteIntegrationChecks.RunBatch`；入口会检查验证工程标识，避免在原工程运行写档与换场景测试。
- 具体路线制作与美术绑定步骤见同目录的《多选项与美术接入指南》。

本次只创建本地 Git 提交，没有 push 或远端同步；原有未跟踪的 `.claude/` 没有纳入提交。
